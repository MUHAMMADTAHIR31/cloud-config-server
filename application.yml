management.endpoints.web.exposure.include: "*"

eureka:
   client:
      serviceUrl:
         defaultZone: http://localhost:9000/eureka
      instance:
      preferIpAddress: true
      hostname: localhost
      
